/*
$(window).bind(
        'load',
        function($)
        {
        	$('#showtimes-container').jScrollPane();
        }
);
*/

$(document).ready( function() {

	// this initialises the demo scollpanes on the page.
	$('#showtimes-container').jScrollPane();
	
	/* to refresh the scrollbar content after selecting a new date*
	$('#showtimes-container').jScrollPane();*/
})// JavaScript Document
